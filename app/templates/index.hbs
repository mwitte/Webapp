{{#each productsGrid}}
    <div class="row">
        {{#each this}}
            <div class="col-sm-3">
                <div class="thumbnail">
                    <img data-src="holder.js/400x300/auto" alt="...">
                    <div class="caption">
                        <h3>{{name}}</h3>
                        <p>...</p>
                        <p>
                            <a href="#" class="btn btn-primary" role="button">Kaufen</a>
                        </p>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
{{/each}}